import React from "react";
import ReactDOM from "react-dom";
import App from "./components/App";

ReactDOM.render(<App />, document.getElementById("root"));

//Up until now in all the apps that we'/e created where we used state
//we'/e tried to keep the component tree /ery simple. In fact all that
//we ha.e is a single App component where e/erything is being rendered.
//Go to App.jsx for further notes
